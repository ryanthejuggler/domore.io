// Generated by CoffeeScript 1.6.2
(function() {
  var MongoClient, ObjectID, collection, connect, db, settings, _ref;

  settings = require('./dbconfig.json');

  _ref = require('mongodb'), MongoClient = _ref.MongoClient, ObjectID = _ref.ObjectID;

  db = {};

  connect = exports.connect = function(next) {
    return MongoClient.connect('mongodb://' + settings.host + ':' + settings.port + '/' + settings.db, function(err, _db) {
      db = _db;
      return next(err, db);
    });
  };

  collection = exports.collection = function(cx, next) {
    return db.collection(cx, next);
  };

  exports.ObjectID = ObjectID;

}).call(this);
